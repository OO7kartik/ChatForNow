<!DOCTYPE html>
<html>

<head>
  <link rel="stylesheet" type="text/css" href="/stylesheets/home.css">
</head>
<body>

<h1>ChatForNow</h1>
<nav class="nav_bar">
  <ul>
  <li><a href="/home">Home</a></li>
  <li style="float:right"><a href="/register">Register</a></li>
  <li style="float:right"><a href="/login">Login</a></li>
</ul>
</nav>


<p id="demo"></p>
<p id="demo2"></p>

<script>
var i = 0;
var j = 0;
let str = '';

var txt = ['Where the world chats...','What are you waiting for?','Sign up now!',' And lets ChatForNow'];

var speed = 100;
var speedb = 30;

let arr=[];

function typeWriter1() {
  if (i < txt[j].length) {
    arr.push(txt[j].charAt(i));
    arr.forEach( (item) => {
      str += item;
    })
    document.getElementById("demo").innerHTML = str;
    str='';
    i++;
    setTimeout(typeWriter1, speed);
  }
}

function reset(){
  j++;
  i = 0;
  arr=[];
}

function typeRemover(){
  if (i > 0) {
    arr.pop();
    arr.forEach( (item) => {
      str += item;
    })
    document.getElementById("demo").innerHTML = str;
    str='';
    i--;
    setTimeout(typeRemover, speedb);
  }
}
setTimeout(typeRemover, speed*31);
setTimeout(reset,(speed+speedb)*60);
setTimeout(typeWriter1, (speed+speedb)*61);

typeWriter1();
</script>

</body>
</html>
